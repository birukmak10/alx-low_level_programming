#include "main.h"

/**
 * main -  a function that computes the absolute value of an intege
 *
 * Return: Always 0.
 */
int _abs(int c)
{
  if(c >= 0)
  {
    return (c);
  }  
  else
  {
    return (-c);
  }
}
